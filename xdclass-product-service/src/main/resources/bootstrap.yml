spring:
  application:
    name: xdclass-product-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.50.101:8848 #Nacos配置中心地址
        file-extension: yaml #文件拓展格式

  profiles:
    active: dev